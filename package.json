{
  "name": "ssg-portfolio",
  "version": "1.0.0",
  "description": "A lightweight static site generator for portfolio and blog websites",
  "main": "build.js",
  "scripts": {
    "build": "npm run clean && node build.js",
    "dev": "nodemon --exec \"node build.js\" --watch contents --watch templates --watch assets --watch config.json",
    "serve": "cd dist && http-server -p 8000 -c-1",
    "start": "npm run clean && concurrently \"npm run dev\" \"npm run serve\" --kill-others",
    "clean": "rimraf dist"
  },
  "keywords": [
    "static-site-generator",
    "ssg",
    "blog",
    "portfolio",
    "markdown",
    "handlebars"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "gray-matter": "^4.0.3",
    "handlebars": "^4.7.7",
    "marked": "^18.0.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "http-server": "^14.1.1",
    "nodemon": "^3.0.1",
    "prettier": "^3.0.0",
    "rimraf": "^6.1.3"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
